import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const etc = () => {
  return (
    <View>
      <Text>etc</Text>
    </View>
  )
}

export default etc

const styles = StyleSheet.create({})